#include "MainForm.h"

// Put something like below example from another program to make this entry point

/*

MainForm::MainForm() {
	InitializeComponent();
	// Register the KeyDown event handler
	this->KeyDown += gcnew KeyEventHandler(this, &MainForm::MainForm_KeyDown);
	richTextBoxCalculation->Text = "";
	richTextBoxAnswer->Text = "";

	operationPerformed = false;
	operand1 = 0;
	operand2 = 0;
	operation = "";
	currentText = "";
}

void main(array<String^>^ args)
{
	Application::EnableVisualStyles();
	Application::SetCompatibleTextRenderingDefault(false);
	Calculator::MainForm form;
	Application::Run(% form);
}
*/